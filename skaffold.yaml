apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/salaboy/spring-cloud-kubernetes:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/salaboy/spring-cloud-kubernetes:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: spring-cloud-kubernetes
        chartPath: charts/spring-cloud-kubernetes
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/salaboy/spring-cloud-kubernetes"
          image.tag: "{{.TAG}}"
